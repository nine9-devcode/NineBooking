# นโยบายความปลอดภัย

โปรเจกนี้เป็นผลงานตัวอย่าง ไม่ได้ให้บริการจริง และไม่มีข้อมูลของผู้ใช้จริงอยู่

ถึงอย่างนั้น ถ้าพบช่องโหว่ในโค้ด รบกวนเปิด issue หรือติดต่อผ่าน GitHub ได้เลย
ไม่ต้องกังวลเรื่องการเปิดเผยแบบมีกำหนดเวลา

## สิ่งที่ต้องรู้ก่อนนำไปใช้จริง

โค้ดชุดนี้ยังไม่พร้อมสำหรับ production ตามที่เป็นอยู่ อย่างน้อยต้องทำเพิ่ม:

- **อีเมล** — ค่าเริ่มต้นเขียนอีเมลเป็นไฟล์ลง `.dev-outbox/` ไม่ได้ส่งจริง
  ต้องเขียน adapter ใน `src/lib/mailer/` ก่อน (ระบบจะไม่ยอมทำงานถ้ายังเป็นโหมดนี้
  บน `NODE_ENV=production`)
- **ไฟล์อัปโหลด** — เก็บลงดิสก์ในเครื่อง บน serverless ไฟล์จะหายเมื่อ deploy ใหม่
- **การแจ้งเตือนแบบ real-time** — เก็บรายชื่อผู้ฟังไว้ในหน่วยความจำของ process
  ใช้ได้กับ instance เดียวเท่านั้น
- **การจำกัดอัตราการเรียก** — เก็บสถานะใน PostgreSQL ถ้าโหลดสูงควรเปลี่ยนเป็น Redis
