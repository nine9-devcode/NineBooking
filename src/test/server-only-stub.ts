/**
 * ตัวแทนของแพ็กเกจ server-only ตอนรันเทสต์
 *
 * ตัวจริงจะโยน error เมื่อถูกโหลดจากฝั่ง client ซึ่งเป็นสิ่งที่เราต้องการตอน build
 * แต่ Vitest รันบน Node ไม่ได้ประกอบบันเดิลเบราว์เซอร์ จึงต้อง alias มาที่นี่
 * (ดู vitest.config.ts) — ไฟล์นี้ตั้งใจให้ว่างเปล่า
 */
export {}
